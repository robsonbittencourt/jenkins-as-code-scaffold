credentials:
  system:
    domainCredentials:
      - credentials:
        - string:
            description: "Example of password credential"
            id: "PASSWORD"
            scope: GLOBAL
            secret: "12345"